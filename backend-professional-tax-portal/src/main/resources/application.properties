
# OTP Settings
otp.expiry.minutes=5
otp.length=6

# server Configuration
server.port=5000
server.address=0.0.0.0
server.address=0.0.0.0

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/ptax
spring.datasource.username=ptax_user
spring.datasource.password=ptax_password
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# CORS Configuration
spring.web.cors.allowed-origins=http://localhost:3000
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true

# OTP Email Service Configuration
otp.email.api.url=https://api.getotp.email/v1/send
otp.email.api.key=lC9wdxLeZVI41b0oXcEgkhW3tGAqDj75
